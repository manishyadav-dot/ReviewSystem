# Layout Components

Application shell and structural components that define page layout.

## Examples (planned)

- `AppShell` — Root layout wrapper with navigation slots
- `Header` — Top navigation bar
- `Sidebar` — Side navigation panel
- `PageContainer` — Consistent page padding and max-width
- `Footer` — Application footer

Layout components define the structural frame; feature content renders inside them.
