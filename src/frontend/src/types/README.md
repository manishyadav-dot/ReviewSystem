# Types

Shared TypeScript type definitions and interfaces.

## Contents (planned)

- `api.types.ts` — API request/response shapes
- `common.types.ts` — Shared utility types (e.g., `PaginatedResponse<T>`)
- `enums.ts` — Shared enumeration values

Feature-specific types should live in `features/{name}/types/` unless used across multiple features.
