# Public Assets

Static files served directly without processing by the build tool.

## Typical Contents

- `index.html` — Application entry HTML
- `favicon.ico` — Browser tab icon
- `robots.txt` — Search engine directives
- `manifest.json` — PWA manifest (if applicable)

Files in this directory are copied to the build output as-is.
