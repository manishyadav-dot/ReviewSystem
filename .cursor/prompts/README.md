# Cursor Prompts

Reusable AI prompts for feature implementation. Each prompt references requirements in the same folder.

| File | Purpose |
|------|---------|
| `Identity_RBAC_Requirements.md` | Requirements summary for Identity & RBAC |
| `identity-rbac-prompts.md` | Step-by-step prompts (backend + frontend) |

Before running prompts: load `.cursor/rules/` and search the codebase.
